import SwiftUI

/// Placeholder for `app/(tabs)/recipes.tsx` (350 ln). See HomeScreen.swift.
struct RecipesScreen: View {
    var body: some View {
        NavigationStack {
            Color(Colors.background)
                .ignoresSafeArea()
                .navigationTitle("Recipes")
        }
    }
}

#Preview {
    RecipesScreen()
}
