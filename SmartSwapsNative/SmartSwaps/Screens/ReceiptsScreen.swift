import SwiftUI

/// Placeholder for `app/(tabs)/receipts.tsx` (467 ln). See HomeScreen.swift.
struct ReceiptsScreen: View {
    var body: some View {
        NavigationStack {
            Color(Colors.background)
                .ignoresSafeArea()
                .navigationTitle("Receipts")
        }
    }
}

#Preview {
    ReceiptsScreen()
}
