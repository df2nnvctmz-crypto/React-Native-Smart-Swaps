const { buildFoodIndex } = require('../app/useFoods'); const fs = require('fs'); const foods = JSON.parse(fs.readFileSync('foods.json', 'utf8')); const idx = buildFoodIndex(foods); console.log('knoten index:', Array.from(idx.index.get('knoten') ? idx.index.get('knoten') : []).map(f => f.name_de)); console.log('knoten stem:', Array.from(idx.stemIndex.get('knot') ? idx.stemIndex.get('knot') : []).map(f => f.name_de));  
