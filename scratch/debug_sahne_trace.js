const { parseReceiptLine } = require('../app/engine/receiptParser'); const { buildFoodIndex } = require('../app/useFoods'); const fs = require('fs'); const foods = JSON.parse(fs.readFileSync('foods.json', 'utf8')); const idx = buildFoodIndex(foods); console.log(parseReceiptLine('GL Sahne 30%% 200g VLOG', foods, idx));  
