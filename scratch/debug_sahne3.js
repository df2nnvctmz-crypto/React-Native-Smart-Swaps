const { parseReceiptLine } = require('../app/engine/receiptParser'); const { buildFoodIndex } = require('../app/useFoods'); const fs = require('fs'); const foods = JSON.parse(fs.readFileSync('foods.json', 'utf8')); const idx = buildFoodIndex(foods); const f1 = foods.find(x => x.name_de.includes('30 % Fett') && x.name_de.includes('Schlag')); const f2 = foods.find(x => x.name_de.includes('36 % Fett') && x.name_de.includes('Schlag')); console.log(f1 ? f1.name_de : 'f1 missing'); console.log(f2 ? f2.name_de : 'f2 missing'); console.log(parseReceiptLine('GL Sahne 30% 200g VLOG', [f1, f2], idx));  
