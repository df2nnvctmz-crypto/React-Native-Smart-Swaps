const { parseReceiptLine } = require('../app/engine/receiptParser'); const { buildFoodIndex } = require('../app/useFoods'); const fs = require('fs'); const foods = JSON.parse(fs.readFileSync('foods.json', 'utf8')); const idx = buildFoodIndex(foods); const f1 = foods.find(x => x.name_de === 'Schlagsahne mind. 30 %% Fett'); const f2 = foods.find(x => x.name_de === 'Schlagsahne mind. 36 %% Fett'); console.log(parseReceiptLine('GL Sahne 30%% 200g VLOG', [f1, f2], idx));  
